<odoo>

    <record id="view_runbot_bundle" model="ir.ui.view">
        <field name="model">runbot.bundle</field>
        <field name="inherit_id" ref="runbot.view_runbot_bundle"/>
        <field name="arch" type="xml">
            <field name="build_all" position="after">
                    <field name="restore_dump"/>
                    <field name="restore_db"/>
            </field>
        </field>
    </record>
</odoo>